use flake
PATH_add bin
# Config de himalaya: así los comandos van a secas (sin -c). expand_path lo
# resuelve respecto a este .envrc, viva donde viva el repo.
export HIMALAYA_CONFIG="$(expand_path config.toml)"
